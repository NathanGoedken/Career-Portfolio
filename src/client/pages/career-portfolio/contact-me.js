import React from 'react';
import Layout from '../../components/layout';

const AboutMe = () =>
    <Layout>
        <div>
            <p>{'hello'}</p>
        </div>
    </Layout>;

export default AboutMe;
